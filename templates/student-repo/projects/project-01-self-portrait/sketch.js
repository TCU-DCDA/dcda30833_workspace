// PROJECT 01 — System Self-Portrait
// DCDA 30833
//
// A rule-based visual system that reflects how you think, move, or organize.
// This is NOT a literal portrait — it's a system identity.
//
// Requirements:
// - Fully deterministic (no random())
// - Uses frameCount for time-based behavior
// - Uses loops, transformations, and/or trigonometry
// - Creates emergent visual behavior from simple rules

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(20);

  // Your system here
}
