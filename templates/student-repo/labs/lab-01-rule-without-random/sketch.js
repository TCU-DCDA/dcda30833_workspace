// LAB 01 — Rule Without Random
// DCDA 30833
//
// Create emergent behavior using ONLY:
// - loops
// - sin() and cos()
// - frameCount
// - translate(), rotate(), scale()
// - arithmetic
//
// NO random(), noise(), or external data allowed.

function setup() {
  createCanvas(400, 400);
  rectMode(CENTER);
}

function draw() {
  background(20);
  let t = frameCount * 0.02;

  translate(width / 2, height / 2);

  // Build your rule-based system here
}
